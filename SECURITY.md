# Security Policy & PDPA — SNC (Smart Nurse Call)

> **ลิขสิทธิ์:** ซอฟต์แวร์และเอกสารประกอบทั้งหมดใน repository นี้เป็นลิขสิทธิ์ของ
> **nithep — Smart Nurse Call (SNC) Project** ตามที่ระบุในไฟล์ `LICENSE`
> (Copyright (c) 2026 nithep — Smart Nurse Call (SNC) Project. All rights reserved.)
> ไฟล์นี้และเอกสารอื่นใน `doc/` อยู่ภายใต้นโยบายเดียวกัน ห้ามคัดลอก นำไปใช้ แต่ผิดวัตถุประสงค์
> หรือเปิดเผยโดยไม่ได้รับอนุญาต

ระบบ SNC ทำงานกับ **ข้อมูลสุขภาพ (PHI)** และ **ข้อมูลส่วนบุคคล (PII)** ของผู้ป่วยและบุคลากร
จึงอยู่ภายใต้ พ.ร.บ. คุ้มครองข้อมูลส่วนบุคคล (PDPA) รวมถึงมาตรฐานความปลอดภัยโรงพยาบาล
เอกสารฉบับนี้กำหนดนโยบายความปลอดภัย ขอบเขตข้อมูล และวิธีปฏิบัติสำหรับผู้พัฒนาและผู้ดูแลระบบ

---

## 1. การรายงานช่องโหว่ (Vulnerability Reporting)

หากพบช่องโหว่ด้านความปลอดภัย กรุณาแจ้งทีมงานโดยตรง **ก่อน** การเปิดเผยต่อสาธารณะ:

- อีเมล: **security@nithep.com** (สำรอง: **admin@nithep.com**)
- ระบุ: รายละเอียดช่องโหว่ ขั้นตอนการทำซ้ำ ผลกระทบ และช่องทางติดต่อกลับ
- ทีมงานจะตอบกลับภายใน **3 วันทำการ** และดำเนินการแก้ไขตามความรุนแรง
- เราเคารพการเปิดเผยแบบผู้วิจัยดี (Coordinated Disclosure) และไม่ดำเนินคดีกับผู้แจ้งที่สุจริต

ห้ามโพสต์ช่องโหว่ที่ยังไม่ได้รับการแก้ไขลง Issue สาธารณะ หรือโซเชียลมีเดีย

---

## 2. การจัดประเภทข้อมูล (Data Classification)

| ประเภท | ตัวอย่าง | เกณฑ์การจัดการ |
|--------|----------|----------------|
| **สาธารณะ (Public)** | หน้า Landing, เอกสารคู่มือ, โค้ด open-spec | ขึ้น git ได้ |
| **ภายใน (Internal)** | Config ทั่วไป, สถิติรวม (aggregate) | ขึ้น git ได้ ห้ามมีความลับ |
| **ลับ (Confidential)** | API Key, Token, คู่มือ rotate key | ห้ามขึ้น git ใช้ Secret Manager / `.env` |
| **PHI / PII (Restricted)** | เหตุการณ์เรียกพยาบาล, หมายเลขห้อง, เวลา, ชื่อ/อีเมลผู้ติดต่อ | **ห้ามเด็ดขาด** ลง git; เก็บใน DB ที่มีสิทธิ์จำกัด |

### 2.1 ข้อมูลสุขภาพ (PHI)
- ฐานข้อมูล `nurse_call_events.db` (และ WAL/SHM) — บันทึกเหตุการณ์เรียกพยาบาลต่อห้อง
- Event outbox ของ PBX listener (`pbx/*.outbox.jsonl`) — อาจมีหมายเลขห้อง/เหตุการณ์ผู้ป่วย
- ไฟล์เหล่านี้ถูกกันไว้ใน `.gitignore` (`*.db`, `*.db-wal`, `*.db-shm`, `pbx/*.outbox.jsonl`)

### 2.2 ข้อมูลส่วนบุคคล (PII)
- `contacts.jsonl` — ฟอร์มติดต่อบน Landing (ชื่อ/อีเมล/ข้อความ/IP)
- ถูกกันไว้ใน `.gitignore` แล้ว ห้าม commit เด็ดขาด

---

## 3. PDPA Compliance

- **Data Minimization:** เก็บเฉพาะข้อมูลที่จำเป็น (ห้อง + เหตุการณ์ + เวลา) ไม่เก็บข้อมูลประจำตัวผู้ป่วยที่ระบุตัวได้ชัดเจน หากไม่จำเป็น
- **Consent:** การเปิดเผยข้อมูลผู้ป่วยผ่านแดชบอร์ด/แจ้งเตือนต้องอยู่ในขอบเขตที่ผู้ใช้บริการตกลง
- **Purpose Limitation:** ใช้ข้อมูลเพื่อการแจ้งเตือนพยาบาลและรายงาน SLA/KPI เท่านั้น
- **Right to Erasure:** สามารถลบ/ลดความเชื่อมโยง (anonymize) เร็คคอร์ดได้ตามคำขอ
- **Retention:** กำหนดอายุการเก็บ (แนะนำรายงานรวม เก็บถาวร, รายการดิบลดความเชื่อมโยงหลัง 30–90 วัน)
- **Cross-border:** หากใช้ Cloud Run (GCP) ข้อมูลอยู่ใน `asia-southeast1` — ต้องแจ้งผู้เกี่ยวข้องเรื่องการประมวลผลบนระบบคลาวด์

---

## 4. การจัดการความลับ (Secrets Management)

- ใช้ **GCP Secret Manager** สำหรับ production (`snc-api-key`, `snc-gemini-api-key`, `snc-telegram-bot-token`, `snc-monitor-webhook-token`)
- บน Pi4 ใช้ตัวแปรสภาพแวดล้อมใน systemd unit (ไม่เขียนลงไฟล์ใน repo)
- คู่มือการหมุนรหัส: ดู `doc/wiki/SNC_API_KEY_ROTATION_GUIDE.md` และ `doc/wiki/GEMINI_API_KEY_ROTATION_GUIDE.md`
- **ห้าม** hardcode รหัสลงในโค้ด เอกสาร คอมมิต หรือไฟล์ `.env` ที่ไม่ถูก ignore
- ใช้ `.env.example` เป็นแค่เทมเพลต (ไม่มีค่าจริง)

---

## 5. Pre-commit Guard (การตรวจรหัส/PHI อัตโนมัติ)

Repository นี้มี hook ตรวจสอบอัตโนมัติก่อน commit ดูใน `ops/git-hooks/pre-commit`
hook จะสแกนไฟล์ที่จะ commit หา:

- กุญแจส่วนตัว (`-----BEGIN * PRIVATE KEY-----`)
- ไฟล์ความลับ (`*.pem`, `*.p12`, `*service-account*.json`, `*credentials*.json`, `.env`)
- รูปแบบ token ที่รู้จัก (AWS `AKIA...`, Google `AIza...`, Slack `xox*-`, GitHub `ghp_/gho_`, OpenAI `sk-`, Telegram `<id>:<token>`)
- การกำหนดค่าเชิงความลับ (`api_key=`, `secret=`, `token=`, `password=` ที่มีค่าจริง ไม่ใช่ placeholder)
- ไฟล์ PHI/PII (`*.db`, `contacts.jsonl`, `pbx/*.outbox.jsonl`)

**เปิดใช้งาน** (รันครั้งเดียวในเครื่อง开发者):

```bash
git config core.hooksPath ops/git-hooks
```

หาก hook บล็อก commit ให้ตรวจสอบว่าคุณเผลอเพิ่มรหัส/ข้อมูลส่วนบุคคลหรือไม่
กรณีที่เป็นเทสต์จริงจำเป็น ให้แก้ไขให้ใช้ placeholder หรือยกเว้นตามข้อควรระวังใน hook
(ห้ามใช้ `--no-verify` เพื่อหลีกเลี่ยงการตรวจสอบ เว้นแต่จะได้รับอนุญาตจากผู้ดูแลระบบ)

---

## 6. การตอบสนองเหตุ (Incident Response)

1. **ตรวจพบ:** แจ้ง `security@nithep.com` ทันที พร้อมหลักฐาน
2. **สืบสวน:** ยืนยันขอบเขต ดู log บน Pi4 และ Cloud Run, ตรวจการเข้าถึง Secret Manager
3. **สกัด:** หมุนรหัสทั้งหมด (ดูคู่มือ rotate), เพิกถอน token, ตัดการเข้าถึงชั่วคราว
4. **แจ้งผู้เกี่ยวข้อง:** หาก PHI รั่วไหล ต้องแจ้งเจ้าของข้อมูล/หน่วยงานตาม PDPA ภายในเวลาที่กฎหมายกำหนด
5. **ปิดบัญชี/แก้ไข:** Deploy แพตช์, บันทึกบทเรียนลง `doc/adr/`

---

## 7. ช่องทางติดต่อ

- ความปลอดภัย: **security@nithep.com**
- ทั่วไป/ฝ่ายเทคนิค: **admin@nithep.com**
- เว็บไซต์โครงการ: https://snc.nithep.com

---

*เอกสารฉบับนี้เป็นส่วนหนึ่งของโครงการ SNC (Smart Nurse Call) โดย nithep
โค้ด เอกสาร และแบรนด์ทั้งหมดสงวนลิขสิทธิ์ตามไฟล์ `LICENSE`*
